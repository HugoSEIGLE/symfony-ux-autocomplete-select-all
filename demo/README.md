# Demo application

This is a real Symfony 6.4 application using AssetMapper. It installs the bundle
from the parent directory through a Composer `path` repository.

```bash
cd demo
composer install
symfony serve
```

Open the URL printed by Symfony CLI. No database is required.
